{
  "name": "nextex-local",
  "private": true,
  "scripts": {
    "setup": "npm --prefix frontend ci && python3 -m venv backend/venv && backend/venv/bin/python -m pip install -r backend/requirements.txt",
    "dev": "node scripts/run.mjs",
    "start": "node scripts/run.mjs --production",
    "build": "node scripts/build.mjs",
    "check": "npm --prefix frontend run check && backend/venv/bin/python -m pytest backend -q",
    "test:e2e": "npm --prefix frontend run test:e2e"
  },
  "engines": { "node": ">=22.12.0" }
}
