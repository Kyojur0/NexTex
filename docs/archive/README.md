# Archive

Historical references; these files are not part of the running application.

- `design-references/`: early mockup and design screenshots.
- `web-app-experiment/`: the earlier Vite prototype.

The maintained application is in `frontend/` and `backend/`.
